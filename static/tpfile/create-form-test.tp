package com.wongnai.web.admin.{packet};

import org.hamcrest.CoreMatchers;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

public class Create{interface}FormTest {{
	private Create{interface}Form underTest;

	@Before
	public void setUp() {{
		underTest = new Create{interface}Form();
	}}

	@Test
	public void testMode() {{
		Assert.assertThat(underTest.getImportMode(), CoreMatchers.equalTo(Create{interface}Form.CREATE_ONLY));
	}}
}}
